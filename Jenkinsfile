pipeline {

    agent any

    stages {

        stage('Build') {
            steps {
		sh 'echo building...'
            }
        }

        stage('Push') {
            steps {
		sh 'echo pushing image...'
            }
        }

        stage('Deploy') {
            steps {
		sh 'deploying image...'
            }
        }
    }
}
