pipeline {

    agent any

    stages {

        stage('Build') {
            steps {
            }
        }

        stage('Push') {
            steps {
            }
        }

        stage('Deploy') {
            steps {
            }
        }
    }
}
